<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/estilosLogin.css">
  <link rel="icon" href="<%= '/logo.png' %>" type="image/png">
  <style>
    /* Style adjustments to make the card more compact */
    .card {
      padding: 1rem;
      height: auto;
      max-width: 400px; /* Set a max width for larger screens */
      margin: auto; /* Center the card horizontally */
    }
    .container {
      min-height: 100vh; /* Make the container full screen height */
      display: flex;
      align-items: center; /* Center vertically */
      justify-content: center;
      padding: 1rem;
    }
    .logo {
      display: block;
      margin: 0 auto 1rem auto; /* Center the logo and add bottom margin */
      max-width: 150px; /* Adjust logo size */
      height: auto;
    }
  </style>
</head>
<body>
  <div class="container">
    <div>
      <img src="/Logotipo.png" alt="Logo" class="logo">
      <div class="opcoes text-center mb-3">
        <button class="botao-login btn btn-secondary mr-2">Login</button>
        <form action="/cadastro" method="GET" style="display: inline;">
          <button class="botao-cadastrar btn btn-secondary">Cadastro</button>
        </form>
      </div>
      <div class="card">
        <div class="card-body">
          <form action="/login" method="POST">
            <div class="form-group">
              <label for="cpf">CPF:</label>
              <input type="text" class="form-control" id="cpf" name="cpf" placeholder="CPF" required>
            </div>
            <div class="form-group">
              <label for="senha">Senha:</label>
              <input type="password" class="form-control" id="senha" name="senha" placeholder="Senha" required>
            </div>
            <button type="submit" class="btn btn-primary btn-block" style="background-color: #9F1416; border: none;">Entrar</button>
          </form>
          <form action="/esqueci" method="GET" class="text-center mt-3">
            <button type="submit" class="btn btn-link botao-esqueci">Esqueceu a senha?</button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>



